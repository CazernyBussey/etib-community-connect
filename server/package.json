{
  "name": "etib-community-connect",
  "version": "3.1.1",
  "private": true,
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "validate:data": "node validate-data.js",
    "check": "node --check server.js && node --check directory-data.js && node --check validate-data.js && node --check ../public/script.js",
    "check:html": "html-validate ../public/*.html",
    "test": "npm run check && npm run validate:data && npm run check:html && node --test test/*.test.mjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "html-validate": "^11.6.0"
  }
}
